#ifndef COMMON_H
#define COMMON_H

/* common headers */
#include <stdio.h>
#include <stdlib.h>
#include <windows.h>
#include <time.h>

/* function prototypes */
void quicksort(int arr[], int left, int right);
int partition(int arr[], int left, int right);

#endif